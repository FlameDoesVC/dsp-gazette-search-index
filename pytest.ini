[pytest]
DJANGO_SETTINGS_MODULE = gazette_search.settings
python_files = test_*.py
testpaths = tests_settings search gazette core tests enrich catalog
addopts = -q --reuse-db -m "not slow"
asyncio_mode = auto
pythonpath = .
markers =
    slow: long-running; run explicitly with -m slow
