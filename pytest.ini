[pytest]
DJANGO_SETTINGS_MODULE = beynunehcheh.settings
python_files = test_*.py
testpaths = tests_settings search gazette ibay core tests enrich
addopts = -q --reuse-db -m "not slow"
asyncio_mode = auto
pythonpath = .
markers =
    slow: long-running; run explicitly with -m slow
